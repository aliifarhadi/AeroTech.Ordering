# S2 - Capacity Reserve, release, expiry and recovery E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S1 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

Execution: use the repository/team-approved stage launch and test mechanism. This runbook defines the required actions/assertions, not the command or environment provisioning.

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Introduce ServicingOperation, CommandReceipt replay, durable ExternalOperation, domain claims and worker fencing.
2. Implement IReservationPort target Reserve/Commit/Release/ReadOperation/ReadResource contract shapes with persistent simulator.
3. Implement canonical Reserve/Release use cases and recovery worker/admin interfaces.
4. Handle married/atomic groups, exact quantities, partial results and all-required-or-release reference disposition.
5. Persist provider request/profile/evidence before and after dispatch; project scope outcomes without rewriting commercial truth.
6. Ship expiry and release now, not deferred to Cancel.

Use the stage's recorded API requests and input data. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: An authoritative reference Order obtains exact scoped capacity evidence and can release/recover it safely after response loss.

## Transactions and restart checkpoints

TX-A receipt+claim+immutable intent; remote effect outside SQL; TX-B exact evidence/state/projection. Response loss never starts a new operation. Release has its own child effect identity.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S2-001`: Intent committed before provider dispatch; exact member/profile/hash persisted
- `SC-S2-002`: Recover same operation, not AlreadyReserved and not another hold
- `SC-S2-003`: New operation allowed; old rejection retained
- `SC-S2-004`: No full confirmed rollup/Issue; unresolved member recovered; successful member not duplicated
- `SC-S2-005`: Confirmed hold released via separate durable effect; commercial sale history remains
- `SC-S2-006`: 2 seat units, not one seat per traveler or exclude every infant
- `SC-S2-007`: Eligibility expires, but resource release remains unknown; no silent owner release
- `SC-S2-008`: Unknown/contract gap unless certified evidence proves effect; no fabricated confirmation
- `SC-S2-009`: Evidence retained, fence blocks A dispatch/advance; claim survives
- `SC-S2-010`: Saved member payload/key/profile unchanged; not rebuilt from current Order
- `SC-S2-011`: Unknown/manual reconciliation, not permission to create another effect
- `SC-S2-012`: One release; original operation read-back resolves; no reacquisition

## Live profile proof

FlightFlow wire DTO work may proceed, but live mutations require BD-002/003. Simulator LOCAL_DONE does not certify current FlightFlow.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
