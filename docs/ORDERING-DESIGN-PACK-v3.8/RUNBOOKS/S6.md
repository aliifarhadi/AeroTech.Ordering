# S6 - Typed ancillary and non-flight product sale E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S5 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

Execution: use the repository/team-approved stage launch and test mechanism. This runbook defines the required actions/assertions, not the command or environment provisioning.

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement source product and pricing preparation, typed validation and explicit source profile snapshots.
2. Add immutable new item/service/membership/price facts and incremental obligations.
3. Support included/complimentary treatment without artificial zero-price lines.
4. Use FlightFlow only for products with flight capacity; use supplier port for independent non-flight booking.
5. Support shared room/vehicle and through-baggage coverage portions without multiplying commercial value.
6. Declare dependency dispositions for subsequent cancellation/change.

Use the stage's recorded API requests and input data. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Seat, baggage, meal, lounge, hotel and transfer products can be accepted with exact beneficiaries/coverage/pricing and appropriate resource owner.

## Transactions and restart checkpoints

Metadata/pricing before explicit acceptance; one local commercial addition TX. Resource acquisition, when required, uses separate persisted effects, not the Create/Add SQL transaction.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S6-001`: One typedservice/newitem/priceline30; FlightFlownevercalled
- `SC-S6-002`: Soldseatproductseparatefromoperationalseat; capacityprotocolusedonlyrequiredscope
- `SC-S6-003`: Oneindependentlycancellable room-stayservice,total300not600/900
- `SC-S6-004`: Onevehicleservice withallbeneficiaries; total80
- `SC-S6-005`: Onebagservice with2coverageportions andunits; no2ndbagcharge
- `SC-S6-006`: Trackserviceifindependentdeliveryneeded; nofabricatedzerocharge
- `SC-S6-007`: 409stale; no silentrebase/reprice
- `SC-S6-008`: Originalsupplierresource/effect retained; no FlightFlowcapacityfake

## Live profile proof

Catalog/supplier BD-008 and pricing BD-007 may remain simulated. Existing real flight capacity is never used for a capacity-free lounge by convenience.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
