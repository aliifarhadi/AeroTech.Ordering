# S4 - Issue ETKT/EMD and document-stock core E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S3 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

Execution: use the repository/team-approved stage launch and test mechanism. This runbook defines the required actions/assertions, not the command or environment provisioning.

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement separate ETKT/EMD/DocumentStock roots and coupon financial/control axes.
2. Separate CanStartIssue from CanCommitDocuments; add durable capacity commit and funding authority phases.
3. Implement Local authority atomic document issuance; test External authority profile with persistent issuer simulation.
4. Enforce stock namespace uniqueness, overlap serialization, reserved-number replay and non-recycling.
5. Persist issuance snapshots/coupon links, outbox events and local projection in final TX.
6. Support core service EMD and explicit fee/value purposes; S7 expands paid-service associations, not a second issuer.

Use the stage's recorded API requests and input data. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Eligible scope obtains one correct local ETKT/EMD set after required capacity commit and funding authority; broker/issuer response loss is recoverable.

## Transactions and restart checkpoints

Required remote authority effects precede local document pivot. Local profile writes entire planned document set, stock allocation, outcome, projection and outbox in one TX. External profile retains per-document evidence before aggregate operation completion.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S4-001`: CanStartIssue true; commit capacity and acquire funding authority; only then write documents
- `SC-S4-002`: No document yet; same commit read-back resolves then issue once
- `SC-S4-003`: Zero committed documents; no first partial local ticket; retry uses coherent transaction
- `SC-S4-004`: Unique numbers under SQL concurrency; no duplicate or random replacement
- `SC-S4-005`: One conflicting registration rejected atomically
- `SC-S4-006`: Same assignednumber/key and one actual document
- `SC-S4-007`: A retained; B recovered same operation; no whole-order fullyissued summary
- `SC-S4-008`: Service references stable service; fee references pricingline, no fake air service
- `SC-S4-009`: Issue succeeds locally; same outboxEventId pending; Ledger not a gate
- `SC-S4-010`: Control uncertainty is explicit; not replaced by one generic coupon status
- `SC-S4-011`: Retain timely valid result; no destructive expiry overwrite

## Live profile proof

Reference local stock is artificial and non-production. Real stock/issuer authorization BD-006; capacity/funding live BD-003/005.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
