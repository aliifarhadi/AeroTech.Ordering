# D0 - actual target inventory

Run locally in the existing target checkout. Clone only when absent, using START-HERE. No business code in this checkpoint.

```powershell
git remote -v
git status --short
git branch --show-current
git rev-parse HEAD
git diff --stat 2f2b9033f675387f68ef7ff95c9d0a575d836fb5 HEAD
dotnet --info
dotnet sln AeroTech.Ordering.sln list
dotnet restore AeroTech.Ordering.sln
dotnet build AeroTech.Ordering.sln --no-restore
```

Capture stdout/stderr/exit for each command, not only the last exit code. An unrelated dirty worktree is preserved and disclosed. Verify actual Domain/Framework references, OrderingDbContext precision, inbox filter/store behavior and Program composition against REVIEW/02; classify new drift rather than overwriting it. Produce the graph, source fingerprint inventory, reading checklist, B0 fix list and D0_CONFORMANT evidence. A known build defect assigned to B0 is not falsely marked build-pass. D0 has no Order E2E by design.
