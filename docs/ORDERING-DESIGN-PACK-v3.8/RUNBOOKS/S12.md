# S12 - DCS observations, control and delivery facts E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S11 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

Execution: use the repository/team-approved stage launch and test mechanism. This runbook defines the required actions/assertions, not the command or environment provisioning.

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement observation envelope+row dedup, persistent unresolved correlation and per-aspect source chronology.
2. Implement explicit source-consumption interpretation, portion quantities and corrections.
3. Ingest facts during active claims; re-evaluate affected servicing rather than discard facts.
4. Integrate required gateway control commands/read-back through same operation protocol.
5. Publish semantic ServiceDelivered/correction facts without inferring delivery from every Boarded message.
6. Prove protected PII handling and immutable historical links.

Use the stage's recorded API requests and input data. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Normalized gateway observations update delivery/control views with correct ordering and corrections while sold truth remains unchanged.

## Transactions and restart checkpoints

Inbox and observations/current facet/projection/outbox commit together per Order. Large source batches split into durable row/work identities. Control mutations persist separate effects outside SQL.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S12-001`: 2observationsbyobservationKey;no1lostor4duplicates
- `SC-S12-002`: BoardingcurrentOffloaded; noCouponUsed inferred
- `SC-S12-003`: Historyretained;currentdoesnotregress
- `SC-S12-004`: Quarantinependingcorrelation; do notguessnearestflight/traveler
- `SC-S12-005`: SoldScheduleunchanged; currentoperationalviewupdated;CVunchanged
- `SC-S12-006`: Accepttruth; markconflictreconciliation; do notdropfact
- `SC-S12-007`: Appendcorrectionlinkedtooriginal; onecorrectedeconomicdeliveryfact
- `SC-S12-008`: Oneportionconsumed; otherunconsumed, noentirebagconsumption
- `SC-S12-009`: Personalpayloadinaccessible; nonPIImonetarydocumentlineageintact; events/logsminimized

## Live profile proof

Gateway/DCS source semantics and control acknowledgment are BD-009. Reference simulators do not authorize actual operational status mapping.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
