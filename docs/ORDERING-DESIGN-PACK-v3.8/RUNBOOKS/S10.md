# S10 - Exchange, reissue and post-pivot cleanup E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S9 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

Execution: use the repository/team-approved stage launch and test mechanism. This runbook defines the required actions/assertions, not the command or environment provisioning.

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement fixed reference Exchange phase graph, not an agent-chosen ordering.
2. Freeze old/new source pricing, dependency/control/version scope and lineage.
3. Reserve new resources; acquire funding/application/residual authority; commit new capacity before issuance pivot.
4. Local profile: apply commercial/doc old-new pivot atomically; External profile: retain actual per-document issuer evidence and reconcile uncertainty.
5. Release old resources/finalize value after irreversible pivot with forward recovery.
6. Reuse unchanged service IDs for price-only replacement; support one-to-many/many-to-one scope.

Use the stage's recorded API requests and input data. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: A source-priced old/new scope can be replaced without losing fare coupling, multiplying money or duplicating documents under failure.

## Transactions and restart checkpoints

Typed effect graph in DOMAIN/09 is mandatory. New acquisitions are separately durable. Exactly one accepted local commercial pivot per group. Post-pivot cleanup never increments commercial version again.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S10-001`: Many-to-onelineage; newstableID; oldservices/pricesretained
- `SC-S10-002`: KeepServiceId; appendnewmembership/pricing; nosecondseat
- `SC-S10-003`: Exactlyoneapprovedtreatmentnet+30; not+150and+30
- `SC-S10-004`: Noolddocumentpivot; compensateonlyknownnewresources
- `SC-S10-005`: Sameoperation/documentroles; do notcancelnewcapacityuntilissueroutcomeknown
- `SC-S10-006`: Newcommercialtruthretained; recoveroldreleaseforward; no reactivationoldticket
- `SC-S10-007`: ExplicitKeep/Replace/Cancel/ManualReview foreachdependency, associationhistorypreserved
- `SC-S10-008`: Value30handledoncebyJetPay; completioneventsnotsecondcredit
- `SC-S10-009`: OwneraffectedPUscopereturnedandexplicitlyaccepted; unchangedserviceIDsretainedasappropriate
- `SC-S10-010`: Reevaluatevector/control; captureobservation; no blindstalepivot

## Live profile proof

Real repricing/issuer/capacity/funding/control contracts must jointly certify the enabled exchange profile.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
