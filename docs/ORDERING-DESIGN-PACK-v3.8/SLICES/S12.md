# S12 - DCS observations, control and delivery facts

## Goal and user-observable result

Normalized gateway observations update delivery/control views with correct ordering and corrections while sold truth remains unchanged.

## Entry gate and mandatory reading

Prerequisites: S11. Read `../DOMAIN/11-DELIVERY-AND-DISRUPTION.md`, `../API-CONTRACTS.md`, `../DOMAIN/08-OPERATIONS-IDEMPOTENCY-AND-RECOVERY.md`, `../GOVERNANCE/03-STAGE-GATES.md` and the command/invariant/scenario indexes. Required prior stages must be LOCAL_DONE, except D0 uses D0_CONFORMANT. A missing future live binding does not permit local semantic deviations.

Before code, record a bounded stage plan using the repository/team-approved reporting convention with the exact commands/invariants/tests/files/layers for THIS slice. It applies the frozen design, not a new design proposal.

## Required work

1. Implement observation envelope+row dedup, persistent unresolved correlation and per-aspect source chronology.
2. Implement explicit source-consumption interpretation, portion quantities and corrections.
3. Ingest facts during active claims; re-evaluate affected servicing rather than discard facts.
4. Integrate required gateway control commands/read-back through same operation protocol.
5. Publish semantic ServiceDelivered/correction facts without inferring delivery from every Boarded message.
6. Prove protected PII handling and immutable historical links.

## Physical layer changes

Domain: the pure invariants and owned records in the primary specification. Application: one canonical handler per listed command plus typed orchestration/ports. Providers: wire ACL or isolated simulator only. Persistence: explicit owned mappings, indexes and enlisted stores. Synchronizer: extend the single OrderProjector. Query: local read projection only. RestApi/Consumers: translate/authenticate then dispatch; no second workflow. ServiceHost: explicit profile composition only.

## API and application rail

- `/service/v1/delivery-observations`

Canonical command IDs: CMD-019. Existing wrappers across surfaces share the handler, access policy and receipt scope. Do not let an internal route become an S2S replacement.

## Persistence, transaction and read-model boundary

Inbox and observations/current facet/projection/outbox commit together per Order. Large source batches split into durable row/work identities. Control mutations persist separate effects outside SQL.

Introduce only this slice's required tables/migrations from DOMAIN/13. Existing accepted records are not rewritten to simplify the migration. Persist raw source evidence separately from normalized facts and protect personal payloads. Projection schema changes remain backward-readable or have an explicit version migration. A failed projection write rolls back its local fact commit.

## Idempotency, concurrency, Unknown and recovery

Follow DOMAIN/08 verbatim for external effects: receipt lookup precedes new eligibility; retain exact effect/profile/request; no external call inside SQL; lease/fence is not a commercial claim; late evidence is retained; authoritative absence needs proof. New local effects use SQL uniqueness and commit-result read-back. Distinguish cancellation of the HTTP wait from cancellation of a business operation.

Every new external mutation needs crash cases before intent commit, after intent commit, after dispatch, after owner commit/response loss, before local outcome commit and after local commit/response loss. D0 is inspection only and B0 applies these to generic infrastructure test messages, not invented sales.

## Events

ServiceDelivered, ServiceDeliveryCorrected

Use EVENT-CATALOG and SPEC/events.json economic roles. CommercialVersion does not count events. Provider movement confirmation does not append an already accepted commercial delta again.

## Acceptance tests

Execute every stage scenario below, plus all applicable earlier regression suites. For a state-changing slice this includes Domain, Application, real SQL Server migration/constraints/concurrency, HTTP authorization/route dispatch, adapter contract tests, projection rebuild and crash/restart. In-memory EF or mocked repositories cannot certify SQL behavior. Verify the filtered run discovered the expected tests; exit code zero with zero tests is failure.

Required scenario IDs: SC-S12-001, SC-S12-002, SC-S12-003, SC-S12-004, SC-S12-005, SC-S12-006, SC-S12-007, SC-S12-008, SC-S12-009.

Introduced invariant IDs: INV-046, INV-047, INV-048, INV-060.

## Live and simulator acceptance

Gateway/DCS source semantics and control acknowledgment are BD-009. Reference simulators do not authorize actual operational status mapping.

Simulated/reference effects are always labeled in evidence. A real HTTP response alone is not authoritative mutation/retention/recovery proof.

## Manual E2E runbook

Execute [RUNBOOKS/S12.md](../RUNBOOKS/S12.md) in the local developer/agent terminal. The runbook states what the Agent must physically deliver and which artifacts are environment inputs. Do not run against production to fill a missing test fixture.

## DONE gate

LOCAL_DONE only after required local scenarios, architecture checks, clean bootstrap/upgrade, API start, persistent outcomes, projection and recovery pass in actual execution. Record live status separately using the live gate below. No stub-only or unit-only completion. Produce end-stage evidence before the stage is considered complete. The user/team controls continuation cadence.

## Artifacts produced at end

Generate the full stage bundle from GOVERNANCE/03, including STATUS.json, actual tested source commit, API/openapi.json and requests.http, migrations/constraint report, provider bindings, results.trx + per-scenario results, E2E transcript/effect counts, fault matrix, architecture graph, blockers and exact RUNBOOK. Mark genuinely inapplicable fields with reasons. No invented commit, provider reference, test pass or manual approval.

In the checkpoint response give the artifact location and reproduction command. Continue to another stage only when the user's current instruction explicitly authorizes sequential continuation; otherwise stop at this checkpoint. A real blocking invariant is reported with its scenario/evidence and blocks only affected dependent work.

## Explicitly not in this slice

- No DCS protocol engine or direct DCS integration in Ordering.
- No sold-schedule overwrite.
- No Boarded = Used shortcut.
