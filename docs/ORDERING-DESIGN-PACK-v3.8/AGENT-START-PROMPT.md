# Agent start prompt - AeroTech.Ordering Day 0 / Pack 3.8

You are implementing a fresh airline Ordering service in the EXISTING repository:
https://github.com/aliifarhadi/AeroTech.Ordering

Use the attached ORDERING-DESIGN-PACK-v3.8 as the target Ordering domain/architecture specification. It supersedes previous FreshBuild/VNext prompts. This is an implementation task, not another architecture proposal. Begin with D0 design/repository conformance, then B0 and the vertical slices in semantic dependency order. Do not infer user/team execution mechanics from the pack.

## Scope discipline

Pack 3.8 is authoritative for Ordering domain semantics, service ownership, invariants, layer responsibilities and transaction/recovery semantics. It is **not** authority for developer environment or team process. Preserve explicit repository/user conventions for API wrappers, event base classes, naming, test-project layout, SQL/broker provisioning, auth harness, scripts and reporting paths unless an Ordering semantic invariant truly conflicts with them. If so, report the conflict; do not silently replace the convention. No new semantic, architectural, public-contract, persistence-identity/cardinality or repository-convention decision may be made by the implementation agent without explicit user approval. Pure code-local mechanics inside an already approved rule are not a new decision.

## First actions - do exactly this

1. Locate the existing target checkout. If absent, clone AeroTech.Ordering branch k8s-stg; do not clone old aliifarhadi/Ordering as the working project. Run git remote -v, git status --short, git branch --show-current, git rev-parse HEAD and git log -5 --oneline in the local developer/agent terminal. Record output. Reviewed bootstrap is 2f2b9033f675387f68ef7ff95c9d0a575d836fb5. Inspect changes since it; do not reset/discard work or create a second solution/repository.
   Compare current `CLAUDE.md` with Pack 3.8 before D0. The user authorizes only this conflict-driven alignment: keep Pack 3.8 as business/domain authority; update stale API-contract file/route examples to point at this Pack; make `BLOCKED_DECISION` require explicit user approval before affected work; describe `Providers.Deterministic` as an approved shell/seam whose adapters are implemented by slices; preserve the existing shared `BaseIntegrationEvent`/OutboxWriter envelope rule. Do not rewrite unrelated DB/dev mechanics, naming, test policy, environment or workflow guidance. Record the exact diff.
2. Make the supplied Pack 3.8 available to the repository without overwriting unrelated work; use the repository/user-approved documentation location. Read README, START-HERE, IMPLEMENTATION-INSTRUCTIONS, GOVERNANCE/01..05, REVIEW/01..05, ARCHITECTURE/01..04, DOMAIN/01..15, CONTRACTS/00..12 and SPEC indexes. In particular read the prior-pack findings BEFORE touching copied framework or business code.
3. Execute the D0 conformance work. Record repository inventory, actual restore/build result, dependency graph, design conformance, exact B0 file dispositions and blockers using the repository/user-approved reporting convention. Do not write Order/domain entities during this initial inventory. A failed baseline build is reported, not hidden by replacing the architecture.
4. Execute SLICES/B0.md in the existing checkout. Do **not** remove Domain->Messages merely because Pack 2.0 said so. Certify the exact type allowlist instead: `AeroTech.Messages.Ordering.Enums.*` plus the existing `BusinessContextType`, `PrincipalType`, and `AuthorizationSurface` caller-context types. Any integration event, provider/owner wire DTO, base transport type, or any additional Messages type is a failure unless the user explicitly approves it. Correct only proven caller-context, monetary mapping and messaging/transaction hazards while preserving established project conventions. Reuse generic framework; do not bulk-copy old business code or migrations. Any genuinely missing generic file copied during B0 needs source commit/path/hash and destination. After B0 the implementation must be independent of all other business repository source code.
5. Deliver the B0 technical guarantees: the existing host/layers build and run under the repository's approved environment, persistence/migrations and messaging semantics are verified, and the required architecture/DI/inbox/outbox behaviors are tested. **Do not choose SQL location, Docker/container setup, broker provisioning, auth test issuer, new script names, new tool/test projects or continuation cadence unless already established by the repository/user.** Publish actual B0 evidence; then follow the user's instruction on whether to continue.

## S1 - first business milestone

Implement PrepareOrderFromOffer -> explicit acceptance of stored digest -> CreateOrderFromOffer -> GetOrder. Current AirOffer Details invokes PriceAsync; use it only BEFORE acceptance. Create must consume the exact stored preparation and perform ZERO owner calls, holds, payment or issue operations. Atomically save receipt/preparation consumption, Order/items/typed services/pricing/change/projection/outbox. Replay the same key; reject changed payload and duplicate preparation consumption; test concurrent requests, restart, read-model rebuild and GetOrder with owners offline.

Use the real AirOffer adapter with the LIVE-CANDIDATE-SANDBOX profile to prove a real candidate reaches Create/Get; this does NOT certify absent owner validity/sales-context guarantees. Report LIVE_CANDIDATE_PROVEN separately from BD-001. Use fully capable reference owner profiles for later local stages. Never relabel unverified real source facts as authoritative to obtain a green demo.

## Non-negotiable architecture and domain rules

Preserve the standing AeroTech.Ordering conventions: Ordering-owned enums under `Contracts/AeroTech.Messages/Ordering/Enums`, semantic external ports under `Domain/Ports/{Area}`, `{X}Aggregate` folder structure, `{Name}CommandHandler` naming and every `*Query` in Query. Domain owns business invariants and may use only the C1 Messages allowlist (`AeroTech.Messages.Ordering.Enums.*` plus the three existing caller-context identity types); it must not use EF/HTTP/provider DTOs/integration events/base transport contracts/broker behavior/configuration. `OwnerAirlineId` remains sourced through `IHomeOperatorProvider`/`ReferenceDataHomeOperatorProvider`, not `ICallerContext`. Application owns canonical use-case choreography and consumes Domain ports. Persistence owns mappings/SQL; Providers implement ACLs; Query is read-only; Synchronizer has one authoritative Order projector; RestApi/Consumers remain thin entry points; ServiceHost is composition. Follow `ARCHITECTURE/01-LAYERS.md` and `SPEC/layers.json` without speculative project-reference rewiring.

No Entitlement v1, no global mutable Order state machine, no duplicate Create/Reserve/Payment/Issue/Cancel rails, no legacy FulfillmentTask/TrafficDocument/PaymentService workflow copied. OrderItem is the sold pricing boundary; typed OrderService identity is stable; pricing/FX/source facts and membership history are immutable and changes append. Reversal sign, allocation, fare construction, shared beneficiaries, legs/segments, document purpose and all version scopes follow DOMAIN exactly.

For every external mutation persist immutable intent/profile/key/exact payload before dispatch; keep network outside SQL; apply authoritative evidence in a separate local TX. Terminal/pending receipt resolution precedes new eligibility. Unknown never means failure/success. Same-operation recovery, per-member outcomes, source read-back and worker fencing are mandatory. Lease expiry is not claim release. Ingest real observations even during claims and re-evaluate affected eligibility.

CanStartIssue and CanCommitDocuments are distinct. Final document issue requires committed required capacity, exact operation-bound funding authority and document/control eligibility. Ledger is async outbox only. Local ETKT/EMD authority uses local atomic stock/document commit; external authority uses certified per-document evidence/recovery. Do not invent JetPay guarantees, FlightFlow read-back, source expiry, issuer rules or ancillary pricing. Required missing owner bindings are BLOCKED_DECISION. The current `Providers.Deterministic` project is a composition/project shell, not an existing library of working business adapters. Implement reference simulators/adapters inside that approved project/seam only when the active slice requires them; do not invent parallel projects and do not claim pre-existing deterministic behavior.

## Every stage must end with evidence

Before code for the stage, record a bounded plan linking exact command/invariant/scenario IDs and changed files/layers using the existing reporting convention. Implement the full slice, not isolated layers. Execute real SQL, API/auth, domain/application, shared adapter contract and crash/restart tests. Re-read actual changed source and test output independently; do not trust an agent report alone.

Produce the stage evidence content required by GOVERNANCE/03 using the repository/user-approved location and format. Include tested source commit, API/openapi and requests, migrations/constraints, provider bindings, actual TRX/scenario results, E2E transcript/effect counts, fault matrix, dependency graph, blockers and exact local RUNBOOK. Do not invent passed tests, commits, approvals, provider refs or live certification. Zero tests or skipped required tests is not LOCAL_DONE.

At every checkpoint report stage/status, source commit, what now runs, the reproduction mechanism actually used, actual test results, real/simulated capability status and blockers. Do not decide on your own whether to pause or continue; follow the user/team instruction. A failed required local invariant blocks dependent work; missing unrelated live bindings do not stop complete reference slices. Do not ask questions answered by this pack or source in the target. When a required choice is genuinely missing, ambiguous or contradictory, record `BLOCKED_DECISION` with evidence and ask the user; do not choose among valid alternatives yourself. Within the currently user-authorized stage/scope, do not stop after another plan when no approval/blocker is required. Starting another stage still requires the continuation permission in GOVERNANCE/05 C12. Do not perform or promise asynchronous/background work.

Semantic changes require the decision/migration/regression record BEFORE code; ordinary conforming private refactors do not need a new design workshop. Production/shared-data changes, owner repository changes and legacy cutover require separate authorization. Preserve the worktree and secrets. Start now with the repository inventory in step 1.
