# S8 - Document Void and explicit VoidAndCancel E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S7 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S8 -Profile Reference -EvidenceDirectory ./artifacts/stages/S8
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement exact issuer void eligibility/deadline and local/external authority paths.
2. Add DocumentOnly vs approved VoidAndCancel plan discriminator.
3. Reuse canonical cancellation/resource/funding modules when the accepted compound plan includes them.
4. Handle external response loss and partial outcomes without number reuse.
5. Emit documentary void event and any separate accepted commercial change with economic-role correlation.

Use the exact generated `artifacts/stages/S8/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Document-only Void and commercial VoidAndCancel have distinct observable outcomes, correct issuer control and no duplicated cancellation/refund.

## Transactions and restart checkpoints

DocumentOnly local pivot changes documents/projection/outbox but not CV or commercial price. VoidAndCancel executes approved child effects and one explicit commercial pivot; irreversible partial results recover forward.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S8-001`: DocVoid andevent; CVandcommercialtotalunchanged; no automaticpayout.
- `SC-S8-002`: Canonicalvoid+singlecommercialpivot, nosecondcancelrail.
- `SC-S8-003`: Noirreversiblevoiddispatch.
- `SC-S8-004`: Suspend/recoveractualdoc; notmarkusableorallocatenewnumber.
- `SC-S8-005`: Knownineligibledenial; notgenericcalendar-dayguess.

## Live profile proof

BD-006 approved void/control profile and real release/funding capabilities are required for live enablement.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
