# B0 - empty service and infrastructure proof

Follow 00-COMMON in the target root. The implementation agent first supplies and tests the local scripts; then record these actual executions:

```powershell
dotnet restore AeroTech.Ordering.sln
dotnet build AeroTech.Ordering.sln --no-restore
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage B0 -Profile Reference -EvidenceDirectory ./artifacts/stages/B0
```

Verify actual host liveness/readiness, denied anonymous access to protected diagnostics, and absence of unfinished business routes. Write/reload high-precision test money/rates through real SQL; prove transaction rollback and exact uniqueness errors. Use a synthetic infrastructure message to crash after durable enqueue, lose a broker acknowledgment and replay through inbox once. Run the duplicate DI/forbidden assembly tests, including the intentionally failing mutant. Start a clean target-only checkout with old Ordering inaccessible. Deliver the full B0 evidence bundle before S1. Do not run old business migrations to make a DB context start.
