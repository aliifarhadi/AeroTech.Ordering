# S5 - Pre-ticket cancellation E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S4 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S5 -Profile Reference -EvidenceDirectory ./artifacts/stages/S5
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement cancellation preparation/acceptance with dependency closure and immutable price treatment.
2. Reuse S2 release/S3 funding release under one typed cancellation coordinator.
3. Reject issued/uncertain-issue scope from pre-ticket flow.
4. Complete reference commercial cancellation only after required releases are authoritatively resolved.
5. Keep original sold lines and append source-approved reversals/penalties once.

Use the exact generated `artifacts/stages/S5/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: A caller can cancel eligible unissued scope and see exact resource/funding release and commercial cancellation without a parallel Withdraw rail.

## Transactions and restart checkpoints

Preparation/claim; independent durable release effects; one local cancellation/price-change/summary/projection/outbox pivot. Pending/Unknown release prevents that reference pivot.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S5-001`: Independent requiredreleases resolve then one commercialcancel pivot.
- `SC-S5-002`: No finalcancel and no capacityre-reserve; same fundingrelease recovery.
- `SC-S5-003`: Reject or require explicit permittednewplan; never silentlyVoid/Refund.
- `SC-S5-004`: No owner calls; local lineage/cancel+price treatment atomic.
- `SC-S5-005`: Return expandeddependentdisposition for explicit acceptance; no dangling activepaidseat.
- `SC-S5-006`: Onecanonicalhandler/claim; at mostonecommercialcancel.

## Live profile proof

Live cancellation depends on approved pricing and release capabilities. Known absence of a resource is distinct from unknown release.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
