# S13 - Traveler correction, OpenAir assignment and revalidation E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S12 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S13 -Profile Reference -EvidenceDirectory ./artifacts/stages/S13
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement correction policy with stable TravelerId and protected before/after data.
2. Separate identity correction from passenger substitution; reject unsupported substitution.
3. Implement OpenAir/Surface handling and approved later concrete flight binding.
4. Reuse resource/funding gates for assigned flight; version accepted commercial binding explicitly.
5. Implement document revalidation only under supported profile; retain original issued snapshot.

Use the exact generated `artifacts/stages/S13/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Authorized identity corrections and document/flight binding servicing preserve history and do not invent seats or fare changes.

## Transactions and restart checkpoints

Correction has one local commercial metadata TX when accepted; documentary changes follow profile-specific effect protocol. Flight binding that changes accepted service definition increments CV/ServiceVersion; purely documentary correction does not fabricate a new sale.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S13-001`: SameTravelerId; explicitcommercialchangeonce; doccorrection/revalidationseparateprofile.
- `SC-S13-002`: Rejectwithoutauthorityreplacement; noorphaninfant.
- `SC-S13-003`: No inventedFlightId/time/capacity;notdeliveryreadyuntilassigned.
- `SC-S13-004`: CommercialbindingchangeCV+1;historicalopenissuedsnapshotretained;documentassociationversioned.
- `SC-S13-005`: Noairseat/couponforthesurfacegap.
- `SC-S13-006`: ExplicitUnsupported/NeedsOwnerPolicy; notsilentidentityswap.

## Live profile proof

Name/control/open-document profiles require approved issuer/owner semantics; missing support is UnsupportedCapability.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
