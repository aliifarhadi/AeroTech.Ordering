# S14 - Whole-traveler Split and related-order links E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S13 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S14 -Profile Reference -EvidenceDirectory ./artifacts/stages/S14
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement whole-traveler/guardian/dependency closure and shared-service partition refusal by default.
2. Freeze partition pricing/owner divide/application authority and stable child/transfer IDs before dispatch.
3. Use bounded source+child SQL transaction for current ownership, paired transfer lines and both projections.
4. Preserve original document order, consumed coupons, original membership/observation identities and current serving owner.
5. Implement related-order links only, not physical commercial merge.

Use the exact generated `artifacts/stages/S14/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Selected whole travelers can move to one child with stable service/document history and balanced resource/value transfers.

## Transactions and restart checkpoints

Deliberate bounded two-Order local TX after required owner transfer proofs; deterministic lock order. No global transaction with FlightFlow/JetPay. Both views and balanced lines commit or neither.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S14-001`: Service/TravelerIDsretained;childCV1/source+1;pairedmoneyconserved.
- `SC-S14-002`: SharedServiceCannotBePartitioned; nosecondroomcopy.
- `SC-S14-003`: UsedcouponstaysUsed;OriginalOrderIdretained;currentservicingownerchanges.
- `SC-S14-004`: No duplicatedapplication;localownershippivotwaitsrequiredproof.
- `SC-S14-005`: RollbackbothOrders/moneylinks;samestablechildIDreservedforretry.
- `SC-S14-006`: No money/capacity/doccopy; originalsremainseparate.
- `SC-S14-007`: Unsupported; requireseparatepricedliabilitytransferdesign.

## Live profile proof

Live divide/application/value profiles BD-012/005; new ownership links alone cannot certify provider transfer.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.


Additional mandatory regression `SC-S14-008`: after120 is split70/50, source/child original-value refund caps are70/50 with combined lineage<=120; independent tender authority still applies.
