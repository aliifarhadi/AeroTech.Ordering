# S15 - Group/charter blocks and idempotent name materialization E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S14 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S15 -Profile Reference -EvidenceDirectory ./artifacts/stages/S15
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement GroupBooking separate root with block-level counters, source contract and independent deadlines.
2. Acquire/allocate/release existing FlightFlow blocks through explicit owner capability.
3. Implement per-row stable import/materialization identity with validation and durable work.
4. Apply exact accepted per-child pricing/deposit transfers; do not repeat charter contract charge.
5. Execute bounded group+one-child local commit with independent row results/retry.

Use the exact generated `artifacts/stages/S15/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: An unnamed group block can materialize named Orders row by row without double-consuming capacity or duplicating deposits.

## Transactions and restart checkpoints

Batch reception captures durable row identities. Each eligible row uses owner effects and one bounded Group+childOrder transaction; completed rows remain stable on retry.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S15-001`: At most5completebothflightallocations; sum15isnot15passengercapacity.
- `SC-S15-002`: Allocateexistingblock; nosecondgeneralsaleseat.
- `SC-S15-003`: 49resultsstable;invalidrowreported;retrydoesnotrecharge/reissue49.
- `SC-S15-004`: Externalbalancedapplicationssum<=500; nocopy500toeach.
- `SC-S15-005`: Oneallocation; SQL/ownerresourceversionpreservescap.
- `SC-S15-006`: No newchartercontractcharge; explicitretailvalueonlyifapprovedpricing.

## Live profile proof

Live group/block/materialization agreement BD-012 and deposit transfer BD-005; reference block profiles remain synthetic.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
