# S3 - Scoped funding coverage and durable authority E2E

## Where and what to run

Use [00-COMMON](00-COMMON.md) in the local target root. Preconditions: S2 completed locally. Use reference-authoritative Orders created through the real S1 HTTP acceptance flow with the reference source, not direct database seeding of a fake fulfilled Order. An explicitly sandbox-only live AirOffer candidate cannot bypass its assurance limitations to reach real side effects.

```powershell
./scripts/Start-Local.ps1 -Profile Reference
./scripts/Test-Stage.ps1 -Stage S3 -Profile Reference -EvidenceDirectory ./artifacts/stages/S3
```

These are required B0-delivered script interfaces. The implementation agent must execute and save the transcript; this design delivery did not run them.

## Operator-visible sequence

1. Implement IFundingCoveragePort required Establish/AcquireIssueAuthority/Finalize/Release/Read methods without inventing JetPay URIs.
2. Persist obligations, exact authority scopes, owner movement refs and outcome evidence.
3. Build persistent owner simulator with cash/guarantee/refund/transfer reservations and conservation checks.
4. Add funding/release API plus background same-operation recovery.
5. Exercise amount, currency, version, reference, expiry and authority check-use races.

Use the exact generated `artifacts/stages/S3/API/requests.http` and input JSON. Populate all scope/operation/document IDs from prior responses. GET the Order and operation after every command and recovery. Confirm the user-observable result: Coverage and payment evidence are visible by obligation; guarantees, cash, new-product collection and release have independent recoverable evidence.

## Transactions and restart checkpoints

Persist funding intent first; JetPay/reference simulator owns funding TX; local evidence/projection transaction after. Acquire authority is a mutation, not a query masquerading as guaranteed issue permission.

Exercise the named fault points applicable to this slice, restart the host/worker, then resume the original client key. Compare authoritative provider effect counts, immutable local facts and current projection. Provider state is read back from its isolated store/API, not from the Ordering response being tested. Repeat local reads with owners unavailable. Do not treat zero discovered tests as success.

## Exact assertions

- `SC-S3-001`: Evidence usable only for exact obligation/scope.
- `SC-S3-002`: ContractMismatch; Issue authority denied despite equal numeric amount.
- `SC-S3-003`: Not accepted as usable evidence.
- `SC-S3-004`: CashApplied0, GuaranteedCoverage120; no fabricated capture.
- `SC-S3-005`: Covered120 not160/200; settlement supersedes guarantee without double application.
- `SC-S3-006`: One owner effect; Issue remains pending until authoritative evidence.
- `SC-S3-007`: No issue from stale query; acquire authority or suspend.
- `SC-S3-008`: Request20 for new scope; no second120 capture.
- `SC-S3-009`: Same authority released/read back; funds cannot be reused while unknown.
- `SC-S3-010`: Existing operation resumes A; new provider cannot impersonate old evidence.

## Live profile proof

JetPay live coverage/money remains BD-005. Named reference funding profile is sufficient only for local stages.

Replace a simulator with a certified real adapter ONLY in the composition/profile binding. Re-run the same relevant semantic and E2E suite with actual owner effects/read-back. Existing operations retain their original provider profile; switching configuration is not a migration of external resources.

## Pass and end-stage output

Every required scenario is PASS, every expected endpoint reaches its canonical handler, SQL constraints/migrations and recovery are verified, and previous regression tests remain green. Provide the full stage evidence bundle and exact local reproduction command before continuing. List each live capability's status and blocker separately. For S16 a reference pass cannot be labeled RELEASE_CERTIFIED without all deployment gates.
